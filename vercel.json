{
  "version": 2,
  "builds": [
    { "src": "index.html", "use": "@vercel/static" }
  ],
  "routes": [
    { "handle": "filesystem" },          // ← 静的ファイルを最優先
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
